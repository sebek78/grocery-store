import React from 'react';
import ReactDOM from 'react-dom';
import './style.scss';

const element = <h1 className="purple">Hello, world!</h1>;

ReactDOM.render(element, document.getElementById('root'));
